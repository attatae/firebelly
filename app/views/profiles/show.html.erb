<div class="page-header">
	<h1><%= @user.full_name %></h1>
</div>

	<p>
    <%= f.label :about_Me %><br/>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.label :tags %><br/>
    <%= f.text_area :tag_list %>
  </p>

<!--IE "My calendar/myevents" vs all events (events page.)" -->

<!--
<% if @events %>
	<% @events.each do |event| %>
		<div class="profile">
			<%= event.title %>
			
			<%= link_to time_ago_in_words(event.created_at) + " ago", event_path(event) %> 
		</div>
	<% end %>
<% end %> -->